define(function (require) {

    return function () {

        var bootstrap = require("bootstrap"),
            $ = require("jquery"),
            firebase = require("firebase"),
            hbs = require("hbs");

    };
});